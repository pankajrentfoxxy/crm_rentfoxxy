# Prompt: Wire LaptopReportPage.jsx to the real backend

> Paste this into Claude Code (or hand it to a backend/frontend dev) in the TrueTech repo.
> Fill in the `<<...>>` placeholders — especially the DB schema section — before running it.

---

## Context

I have a working **frontend mockup** at:

```
frontend/src/features/reporting/pages/LaptopReportPage.jsx
```

It currently renders entirely from hardcoded mock data (`ROWS`, generated by `seedRow()`). I want to keep the **exact UI/UX, layout, filters, popup behavior, and visual design** of this component, but replace the mock data layer with real data from our database, fetched through a new backend API.

Do not redesign the UI. Do not change component structure, styling, or interaction patterns unless a change is strictly required to support live data (e.g., loading/error states). Treat the current file as the source of truth for what the page should look and behave like.

## What the page currently does (for reference)

- **Filter bar (2 rows):** search (TTSPL/ticket/serial/model), Stage, Team, Technician, Status, date mode (All/Today/Yesterday + custom from–to), and a second row of laptop attribute filters (Brand, Model, Processor, Generation, RAM, SSD).
- **Dashboard summary cards:** Total Tickets (for the selected date range), Ready, Pending, In Progress, Completed, QC Failed, Diagnosis Failed.
- **Stage Summary:** one card per production stage (Floor Manager, QC Queue, Diagnosis, Assembly & Software, Final Testing, Chip Level Repair, Body & Paint, QC1, QC2, Dispatch QC, Inventory), each showing Total / Done / Pending counts.
- **Technician Summary table:** Technician, Total, Ready, In Progress, Done, Pending — one row per technician.
- **Configuration Summary table:** Processor, Count.
- **Every count on the page is clickable** and opens a popup modal listing the matching laptops, with the following columns: Ticket ID, TTSPL, Serial Number, Brand, Model, Processor, Generation, RAM, SSD, Current Stage, Assigned Technician, Team, Ticket Status. The popup has search, pagination (8 rows/page), and CSV export.
- **All filters apply globally** — every card, table, and popup reflects the currently selected filter state.

## Database schema

<<PASTE THE ACTUAL SCHEMA HERE — table names, columns, types, and relationships for tickets/laptops, stages, technicians, teams, and status history. If you don't have it typed up yet, I can generate a proposed schema instead — just say so and I'll draft one from the component's data shape.>>

Example of the level of detail I need if you're pasting a real schema:
```
tickets
  id                bigint PK
  ttspl_id          varchar
  serial_number     varchar
  brand             varchar
  model             varchar
  processor         varchar
  generation        varchar
  ram               varchar
  ssd               varchar
  stage_id          FK -> stages.id
  team_id           FK -> teams.id
  technician_id     FK -> technicians.id
  status            varchar / enum
  created_at        timestamp
  updated_at        timestamp

stages
  id, key, label, sort_order

technicians
  id, name, team_id

teams
  id, name
```

## What I need built

### 1. Backend API endpoints

Design and implement REST (or GraphQL, match our existing convention in `<<backend folder path>>`) endpoints that support:

- `GET /api/reports/laptop-report/summary` — returns Total/Ready/Pending/In Progress/Done/QC Failed/Diagnosis Failed counts for the current filter set.
- `GET /api/reports/laptop-report/stages` — returns per-stage `{ key, label, total, done, pending }` for the current filter set.
- `GET /api/reports/laptop-report/technicians` — returns per-technician `{ name, total, ready, inProgress, done, pending }` for the current filter set.
- `GET /api/reports/laptop-report/configurations` — returns per-processor `{ label, count }` for the current filter set.
- `GET /api/reports/laptop-report/tickets` — returns the paginated, searchable list of tickets matching the current filters **plus** whichever specific popup criteria was clicked (stage, status, technician, or processor). This backs the popup modal. Support `page`, `pageSize`, `search`, and all filter query params.
- `GET /api/reports/laptop-report/tickets/export` — same filters as above, returns a CSV (or the full matching row set for the frontend to convert — match whichever pattern our other export endpoints already use).

All endpoints must accept the same set of filter query params: `dateMode` (`all|today|yesterday|custom`), `dateFrom`, `dateTo`, `stage`, `team`, `technician`, `status`, `brand`, `model`, `processor`, `generation`, `ram`, `ssd`, `search`.

Do all aggregation (counts, group-by) in the database query, not in JS after fetching every row — these numbers need to stay fast as the tickets table grows. Use indexes on `stage_id`, `technician_id`, `status`, and `created_at` if they don't already exist.

### 2. Frontend wiring

- Replace the `ROWS`/`seedRow()` mock data and the client-side `useMemo` aggregations (`baseRows`, `summaryCounts`, `stageAgg`, `techAgg`, `configAgg`) with real API calls (use `<<our existing fetch/axios wrapper, e.g. src/lib/api.js>>`).
- Debounce the search input and re-fetch on filter change (don't re-fetch on every keystroke).
- Add loading and empty/error states for each section (cards, stage grid, technician table, config table, popup) consistent with our existing patterns elsewhere in the app (`<<point to an existing example component if we have one>>`).
- The popup modal (`PopupModal`) should fetch its row list from `/tickets` using the clicked criteria + current filters, rather than filtering an in-memory array. Keep its existing search/pagination UI, just back it with server-side paginated data (`page`, `pageSize`, `total` from the API response).
- Export button should call `/tickets/export` (or generate the CSV client-side from the currently loaded popup page — tell me which if our other exports already do it one way).
- Keep all existing color tokens, typography, and layout exactly as-is.

### 3. Things to double check

- Confirm how "stage" is stored — as a foreign key to a `stages` table (preferred) or a free-text column — and normalize the frontend's stage filter dropdown accordingly.
- Confirm what "Team" maps to in the real schema — is it the technician's team, or a per-ticket assignment separate from stage?
- Confirm whether "Done" is a literal status value or derived (e.g., `completed_at IS NOT NULL`).
- Confirm timezone handling for "Today"/"Yesterday" — should this use the server's timezone, IST, or the browser's local time?

## Deliverable

1. New backend route file(s) + controller/service logic for the five endpoints above.
2. Updated `LaptopReportPage.jsx` wired to real data, with loading/error states, no visual regressions from the current mockup.
3. A short note on any schema assumptions you had to make so I can correct them if wrong.
