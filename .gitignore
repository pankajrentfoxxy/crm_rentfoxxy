node_modules/
.env
.env.local
.DS_Store
*.log
coverage/
build/
build_new/
build_old_*/
dist/

# /migration
# Local uploads (PO PDFs, vendor docs)
backend/uploads/

# Migration runtime artifacts
migration/logs/
migration/.env
migration/migration-validation-baseline.json

# Sale-in-place invoices: private by design, never served statically
backend/private-uploads/

# Production data dumps and generated reports - customer PII, never commit
backups/
reports/
